# =============================================================================
# Server Configuration
# =============================================================================

# Environment (set to 'production' in production)
NODE_ENV=development

# Server port
PORT=3000

# =============================================================================
# Security (REQUIRED in production)
# =============================================================================

# JWT signing secret - generate with: openssl rand -base64 32
# REQUIRED in production - server will fail to start without this
JWT_SECRET=your-secret-key-change-in-production

# API key for client authentication - generate with: openssl rand -base64 32
# REQUIRED in production - server will reject all requests without this
API_KEY=your-api-key-generate-with-openssl-rand-base64-32

# Allowed CORS origins (comma-separated)
# In production: required if clients access from different origin
# Leave empty to reject all cross-origin requests in production
CORS_ORIGINS=https://your-app.example.com

# =============================================================================
# User Allowlist (at least one recommended)
# =============================================================================

# Approved email addresses (comma-separated, case-insensitive)
APPROVED_EMAILS=user1@example.com,user2@example.com

# Approved Apple user IDs (comma-separated) - alternative to email approval
# APPROVED_APPLE_IDS=001825.xxxxx.xxxxx

# =============================================================================
# Apple Sign In
# =============================================================================

APPLE_TEAM_ID=your-team-id
APPLE_BUNDLE_ID=com.ovrlrd.app

# =============================================================================
# APNs Push Notifications (optional)
# =============================================================================

APNS_KEY_ID=your-key-id
APNS_KEY_PATH=./apns-key.p8

# =============================================================================
# Paths
# =============================================================================
# NOTE: Use absolute paths to avoid issues when server is started from
# different directories (e.g., via IDE, scripts, or background processes)

# SQLite database location
DB_PATH=/path/to/server/ovrlrd.db

# Claude CLI executable path
CLAUDE_PATH=claude

# Debug log file location
LOG_PATH=/path/to/server/debug.log

# APNs key path (if using push notifications)
# APNS_KEY_PATH=/path/to/server/apns-key.p8

# =============================================================================
# Claude CLI Settings
# =============================================================================

# Working directory for Claude CLI (defaults to $HOME)
# CLAUDE_WORK_DIR=/Users/yourname

# Additional directories Claude can access (colon-separated absolute paths)
# These are passed as --add-dir flags to the Claude CLI
CLAUDE_ADDITIONAL_DIRS=/Users/yourname/Developer:/Users/yourname/Documents

# =============================================================================
# Debugging
# =============================================================================

# Enable debug-level logging (set to 1 to enable)
# DEBUG=1
